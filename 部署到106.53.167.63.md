# 🚀 部署到腾讯云服务器 - 定制命令

## 你的服务器信息
- **公网 IP**: 106.53.167.63
- **SSH 端口**: 22
- **用户名**: root

---

## 🎯 立即开始部署（3步完成）

### 步骤 1：上传项目到服务器

打开 Git Bash 或 PowerShell，运行：

```bash
cd "C:\Users\22591\Desktop\婚礼ai视频"
bash upload-to-server.sh
```

按提示输入：
```
请输入服务器 IP 地址: 106.53.167.63
请输入 SSH 端口 (默认 22): 22
```

然后输入你的服务器密码。

**上传完成后会显示**：
```
✅ 文件已上传到服务器！
```

---

### 步骤 2：SSH 连接到服务器

```bash
ssh root@106.53.167.63
```

输入密码登录。

---

### 步骤 3：运行自动部署脚本

登录服务器后，运行：

```bash
cd /www/wwwroot/wedscene-ai
bash deploy-to-server.sh
```

脚本会自动：
- ✅ 安装 Node.js 18
- ✅ 安装 PM2
- ✅ 安装项目依赖
- ✅ 配置环境变量（会提示你编辑 .env）
- ✅ 启动服务
- ✅ 配置防火墙

**重要**：脚本会提示编辑 .env 文件，请修改：
```bash
PUBLIC_BASE_URL=http://106.53.167.63:5173
```

---

## 📝 完整命令清单（复制即用）

### 本地操作
```bash
# 进入项目目录
cd "C:\Users\22591\Desktop\婚礼ai视频"

# 上传到服务器
bash upload-to-server.sh
# 输入: 106.53.167.63
# 输入: 22
# 输入: 你的密码
```

### 服务器操作
```bash
# SSH 连接
ssh root@106.53.167.63
# 输入密码

# 进入项目目录
cd /www/wwwroot/wedscene-ai

# 运行部署脚本
bash deploy-to-server.sh

# 脚本会提示编辑 .env，修改这一行：
# PUBLIC_BASE_URL=http://106.53.167.63:5173
```

---

## 🔒 配置腾讯云安全组

部署前，请确保腾讯云安全组已开放端口：

### 在线配置
1. 访问：https://console.cloud.tencent.com/cvm/instance
2. 找到 IP 为 106.53.167.63 的服务器
3. 点击"更多" → "安全组" → "配置安全组"
4. 添加入站规则：

| 类型 | 协议端口 | 来源 | 策略 |
|------|----------|------|------|
| 自定义 | TCP:22 | 0.0.0.0/0 | 允许 |
| 自定义 | TCP:80 | 0.0.0.0/0 | 允许 |
| 自定义 | TCP:443 | 0.0.0.0/0 | 允许 |
| 自定义 | TCP:5173 | 0.0.0.0/0 | 允许 |

---

## ✅ 部署完成后的验证

### 1. 检查服务状态
```bash
pm2 status
```
应该看到 `wedscene` 服务状态为 `online`

### 2. 查看日志
```bash
pm2 logs wedscene
```
应该看到：
```
Motion Video: omni-flash via https://llm-api.net/v1/video/create
（PUBLIC_BASE_URL=http://106.53.167.63:5173）
```

### 3. 浏览器访问
打开浏览器访问：
```
http://106.53.167.63:5173
```

应该能看到婚礼 AI 视频生成系统的登录页面。

---

## 🌐 （可选）配置域名

如果你有域名，可以配置：

### 1. 域名解析
在你的域名管理面板添加 A 记录：
```
类型: A
主机记录: @
记录值: 106.53.167.63
TTL: 600
```

### 2. 配置 Nginx
```bash
# 安装 Nginx
yum install -y nginx
systemctl start nginx
systemctl enable nginx

# 配置反向代理
nano /etc/nginx/conf.d/wedscene.conf
```

粘贴以下内容（将 your-domain.com 替换为你的域名）：
```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;
    
    client_max_body_size 100M;
    
    location / {
        proxy_pass http://localhost:5173;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        proxy_connect_timeout 600s;
        proxy_send_timeout 600s;
        proxy_read_timeout 600s;
    }
}
```

保存后：
```bash
nginx -t
systemctl restart nginx
```

### 3. 配置 SSL（免费 HTTPS）
```bash
yum install -y certbot python3-certbot-nginx
certbot --nginx -d your-domain.com -d www.your-domain.com
```

### 4. 更新 PUBLIC_BASE_URL
```bash
nano /www/wwwroot/wedscene-ai/.env
```
修改为：
```bash
PUBLIC_BASE_URL=https://your-domain.com
```

重启服务：
```bash
pm2 restart wedscene
```

---

## 🐛 常见问题

### 无法连接服务器
```bash
# 测试连接
ping 106.53.167.63

# 测试 SSH
ssh -v root@106.53.167.63
```

### 端口被占用
```bash
# 查看端口占用
netstat -tlnp | grep 5173

# 杀死进程
kill -9 进程ID
```

### PM2 启动失败
```bash
# 查看详细日志
pm2 logs wedscene --lines 50

# 手动测试
cd /www/wwwroot/wedscene-ai
node server.mjs
```

---

## 📞 需要帮助？

如果遇到问题：
1. 查看 PM2 日志：`pm2 logs wedscene`
2. 参考文档：腾讯云部署指南.md
3. 技术支持微信：zjj003831

---

**准备好了吗？复制上面的命令，开始部署！** 🚀

部署完成后访问：**http://106.53.167.63:5173**
